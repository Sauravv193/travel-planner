# WanderGen Frontend — Environment Variables
# Copy this to .env.local with real values (never commit .env.local)
# For Vercel: set VITE_API_URL in Vercel Dashboard

VITE_API_URL=http://localhost:8080/api
